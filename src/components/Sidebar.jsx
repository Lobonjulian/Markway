import Title from '@components/ui/Title';

const Sidebar = () => {
  return (
    <section>
      <Title level={3} title={'Bienvenidos a Markway'} description={'🚧 En construcción... 🚧'} />
    </section>
  );
};

export default Sidebar;
